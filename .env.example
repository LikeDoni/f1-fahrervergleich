# Basis-URL des Backends (json-server).
# Lokal:
VITE_API_URL=http://localhost:3001
# In Produktion (GitHub Pages) wird stattdessen die Render-URL verwendet,
# siehe .github/workflows/deploy.yml bzw. render.yaml.
